# Fail-open pointer

GitHub Issue #3 owns this compatibility path. The canonical fail-open and
blackhole behavior is [section 11 of the WLOC PoC threat model](WLOC_THREAT_MODEL.md#11-精确-fail-open-与-blackhole-行为).

Do not define a second fail-open state machine in this pointer file.
